# GoReleaser owns the CLI binaries, archives, checksums and the GitHub
# Release + changelog. The container image is built separately in
# .github/workflows/release.yaml from the repo's own Dockerfile, so the
# published image stays byte-for-byte what scripts/itest.sh docker tests.
version: 2
project_name: unifi-emu

before:
  hooks:
    - go mod tidy

builds:
  - id: unifi-emu
    main: ./cmd/unifi-emu
    binary: unifi-emu
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.buildVersion={{ .Version }}
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    mod_timestamp: "{{ .CommitTimestamp }}"

  # The herder and the public synthetic image are built from the same tag, so
  # the binary carries the matching image reference rather than resolving one
  # at runtime. These ldflags stay authoritative even though the herder can
  # also recover the reference from build info, because the `go mod tidy` hook
  # above can leave the tree dirty and reduce the recorded module version to
  # one that names no published image. A build from a working tree has no
  # default and requires --synthetic-image. No path falls back to a floating
  # tag.
  - id: unifi-emu-herder
    main: ./cmd/unifi-emu-herder
    binary: unifi-emu-herder
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.buildVersion={{ .Version }}
        -X main.defaultSyntheticImage=ghcr.io/jamesbraid/unifi-emu:{{ .Version }}
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    mod_timestamp: "{{ .CommitTimestamp }}"

archives:
  - id: unifi-emu
    ids: [unifi-emu]
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
  - id: unifi-emu-herder
    ids: [unifi-emu-herder]
    formats: [tar.gz]
    name_template: "unifi-emu-herder_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

# unifi-emu uses kernel/Go-style component-prefixed commits (cli:, emu:,
# image:, inform:, docs:), not Conventional Commits — so no feat/fix
# grouping. Let GitHub render the changelog; just drop the noise.
changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - '^Merge '
      - '^docs:'
      - '^gitignore:'

release:
  github:
    owner: jamesbraid
    name: unifi-emu
  prerelease: auto
  draft: false
  header: |
    ## unifi-emu {{ .Tag }}
  footer: |
    ### Install

    Library:
    ```
    go get github.com/jamesbraid/unifi-emu@{{ .Tag }}
    ```

    CLI:
    ```
    go install github.com/jamesbraid/unifi-emu/cmd/unifi-emu@{{ .Tag }}
    ```

    Herder:
    ```
    go install github.com/jamesbraid/unifi-emu/cmd/unifi-emu-herder@{{ .Tag }}
    ```

    Herder in GitHub Actions:
    ```
    - uses: jamesbraid/unifi-emu/.github/actions/install-herder@{{ .Tag }}
    ```

    Container image:
    ```
    docker pull ghcr.io/jamesbraid/unifi-emu:{{ trimprefix .Tag "v" }}
    ```
